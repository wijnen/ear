file-new

color-code changed db (????)

edit start/end times in UI

(remove split)/(join fragments)

export

Make group selections work better for 'play selection' (check for children)

Annotations

Allow splitting of fragments from the Wt video widget

Render audio as image --> Implemented, but crashes on Debian based systems due to old library. Todo


Fix the filtering of tracks and make the whole search thing in a seperate window --> Fuzzy string search works, filtering needs to be re-implemented that way

Optional: look at styling

When playing a selection containing an expanded group marker, nothing happens.
